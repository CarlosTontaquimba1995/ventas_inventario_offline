name: ventas_inventario_offline
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.14.1    
  sqlite3_flutter_libs: ^0.5.18
  path_provider: ^2.1.1
  path: ^1.8.3
  uuid: ^4.1.0
  provider: ^6.0.5
  connectivity_plus: ^5.0.2
  intl: ^0.18.1
  http: ^1.2.0
  supabase_flutter: ^2.0.2
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  drift_dev: ^2.14.1     
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
